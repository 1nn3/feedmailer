
=pod

=encoding utf8

=head1 NAME

newspaper-helper - bereite zur Zeitung auf

=head1 SYNOPSIS

B<newspaper-helper> <options> -- <URI…>

	export APP_FEEDMAILER_HOOK="/usr/bin/perl -E 'print (\"%l\\n\")'"
	export USER=${USER:-$LOGNAME}
	feedmailer-cronjob "$@" | unsortuniq | newspaper-helper -s -N -F | mail -E -s "🗞️ Newspaper" $USER

=head1 DESCRIPTION

Prints entries of feeds in headlines based on a RE list.

=head1 OPTIONS

=over

=item --version

=item --help

=item -C

Do not use the canonical link in output.
	
=item -x <template>

Template file.

=item -a

News in jede passende Headline einsortieren, nicht nur die erste.

=item -w path/to/whitelist.text

Whitelist filter aka. the headlines. Linewise RE.

=item -b path/to/blacklist.text

Blacklist filter. Linewise RE.

=item -F

Ignore filters. See I<-w> and I<-f> options.

=item -z

Print also emtpy headlines, such with zero matches.

=item -N

Do not print headlines at all.

=item -i FILE

URIs werden zeilenweise aus I<FILE> gelesen.

Not implemented yet.

=back

=head1 FILES

=over

=item whitelist.text, blacklist.text

The Headlines.

Filter: Datei mit Regulären Ausdrücken, auf die ein Artikel passen muss.

When I<-F> is given then the RE I<.*> is assumed.

=back

=head1 SEE ALSO

L<newspaper-cronjob>, L<feedmailer-cronjob>

=cut

