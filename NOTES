NOTES

apt-get install locales
dpkg-reconfigure locales

wget -O - http://ftp.debian.org/debian/dists/stable/main/{binary-all,binary-amd64,binary-i386}/Packages.gz | zcat | awk '$1=="Homepage:"{print $2}' | sort -u

